import { CourseStatusReport.Service } from './course-status-report.service';

describe('CourseStatusReport.Service', () => {
  it('should create an instance', () => {
    expect(new CourseStatusReport.Service()).toBeTruthy();
  });
});
