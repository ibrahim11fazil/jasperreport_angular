import { TacCourseAttendance } from './tac-course-attendance';

describe('TacCourseAttendance', () => {
  it('should create an instance', () => {
    expect(new TacCourseAttendance()).toBeTruthy();
  });
});
