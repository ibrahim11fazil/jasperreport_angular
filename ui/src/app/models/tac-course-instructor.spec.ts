import { TacCourseInstructor } from './tac-course-instructor';

describe('TacCourseInstructor', () => {
  it('should create an instance', () => {
    expect(new TacCourseInstructor()).toBeTruthy();
  });
});
