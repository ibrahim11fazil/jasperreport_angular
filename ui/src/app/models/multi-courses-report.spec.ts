import { MultiCoursesReport } from './multi-courses-report';

describe('MultiCoursesReport', () => {
  it('should create an instance', () => {
    expect(new MultiCoursesReport()).toBeTruthy();
  });
});
