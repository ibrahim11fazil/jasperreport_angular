import { TrainingGuidelines } from './training-guidelines';

describe('TrainingGuidelines', () => {
  it('should create an instance', () => {
    expect(new TrainingGuidelines()).toBeTruthy();
  });
});
