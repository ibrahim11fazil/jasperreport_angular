import { SmartProfileModel } from './smart-profile-model';

describe('SmartProfileModel', () => {
  it('should create an instance', () => {
    expect(new SmartProfileModel()).toBeTruthy();
  });
});
